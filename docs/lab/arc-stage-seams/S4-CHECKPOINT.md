# S4 local-axis checkpoint

Frozen S0-S3 untouched. New computation only under S4/exp/axis-local. This commit retains the calculation, production-gateway Tick/PCR contract, and six passing synthetic tests. LAB warm-runner/grade registration is in the following integration checkpoint; there is deliberately no clean/default S4 contract.

Source-only development execution covered all six packaged courses. Local resolutions: AlexClark 12, DashsTrack 15, Heritage 14, Lenard 17, NorthPark 15, TowneLake 16. All six overlaid course views were opened as a contact sheet. These are NaiveGate selections, not six-course correctness scores.

The generated blind referee verified the exact source hash for IMG_5641 (DashsTrack). S3 produced 18 Tees: 15 localized, three false map-control detections, H3/H5/H12 unlocalized. S4 resolved those 15 correctly, zero wrong assignments; H3/H5/H12 remain unresolved. No raw Annotation coordinates were exported. Visibility metadata is absent, so missing truth targets cannot be attributed to S3 rather than recovery. Unused upstream S3 false detections must not be scored as S4-assigned endpoints.

Other three annotation sources do not currently match packaged source byte hashes, dimensions+RGBA hashes, or executed S0 canonical imageIds. Their semantic grading is BLOCKED, not passed. They were still executed and visually inspected. Do not fit a transform to hidden endpoint coordinates.

Active hypothesis: reciprocal nearest within the five-degree bidirectional axis window retires useful local identities before Basket assignment. Falsifier: wrong semantic association or visible contradictory geometry. It does not claim to solve recovery, straight/bent classification, or paths. Preserve all pair measurements and unresolved holes; never rematch losers just to reach 18.

Packaging HUH: node_modules/.bin/tsc is a flattened executable with a broken relative require. npm build failed. Direct installed compiler invocation succeeded: node node_modules/typescript/bin/tsc -p packages/alg/tsconfig.json, followed by node packages/alg/scripts/copy-assets.mjs. No install or frozen-code repair.
