# Open-frame S4 checkpoint — 2026-09-05

Experiment remains under exp/, no clean Stage edits or promotion. This checkpoint retains the ALG challenger; LAB runner, referee, and warm View integration follow separately. Current remote build is type-compatible with the widened axis-local input types in this commit.

## Observed and tested

AlexClark source H10: a C-shaped bright border plus a disconnected 18-pixel side bar. Ring-only discovery loses their relationship. Minimum-area pose over their composed exact pixels gives a useful rectangle. NorthPark H5: a small white fragment remains beside the Basket neck after exact ownership subtraction; it is NOT recovered by this experiment.

Hypothesis: three supported rectangle sides plus a fourth-side fragment can identify open/disconnected Tees without requiring a closed bright ring. Falsifier: a wrong semantic association or a protected association regression. Existing local associations are retained and removed from the remaining recovery problem.

Results (source-only whole-course images inspected): AlexClark 12->13 (H10); DashsTrack 15->18 (H3,H5,H12); Heritage 14->14; Lenard 17->17; NorthPark 15->16 (H18); TowneLake 16->17 (H13). These are selected local associations, not corpus truth scores.

Generated blind referee: DashsTrack/IMG_5641 exact source-byte identity verified, S4 18 correct, zero wrong/unresolved at declared 7px tolerance. S3 still has 15 localized Tees plus three false map-control detections; its three missing truth targets have UNKNOWN visibility. S4 compensation does not mark S3 solved. Other three annotation cases remain frame-custody blocked; no hidden coordinates were exposed or used for alignment. No S5-S7/path acceptance.

## Rejected measurements retained in decision history

1. Restricting composed parts to seed-sized components excluded the detached bar. Rejected: seed eligibility must not erase a smaller component's membership in the Molecule.
2. A fixed 1.7px rim counted thick border paint as interior. Rejected: test a central void and scale-relative edge support instead. These are explicit NaiveGates, not universal Tee laws.

Replay: ./lab sweep --through S4 --warm --exp open-frame SOURCE_IMAGE (requires following runner integration).

Next discriminating probe: NorthPark H16 falls just outside 5 degrees using bright-component PCA. Refit its existing pixel-cell rectangle before changing the angular NaiveGate. S5 still needs local candidate-Basket geometry and a real course-domain sensor, not global perfect assignment.
