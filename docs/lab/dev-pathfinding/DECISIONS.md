# Dev6 pathfinding — decision journal

## 2026-09-05 — checkpoint custody

Base: `lab/world` at `8d6ca111e6ff5a095950e228b61395dae29e2856`.
Working branch: `lab/dev-pathfinding`. Keep `lab/world` and S0–S3 clean surfaces unchanged.
Corpus reference recorded earlier: `samuelpmahan/chainspot-corpus` at `2b5913d3f1f6d8f97b0324721a4c5201bd3ed819`, `dev/`.

Objective: get every ALG stage working for every dev course; then establish non-vacuous inboundsPathfinding (every expected hole has a path, and every path segment is legal). Only freeze a solution after complete corpus proof. Thereafter keep Geometric Perfection and Kerchoooo as separate experiments, not a blended architecture.

HUH: expected the preceding working checkout and dev-checkpoint receipt to survive, encountered only selected source files, source images, rendered investigation images, the S3 contact packet, and the original packaged workspace. No new implementation or full Dev6 checkpoint receipt was recoverable locally at resume. Do not describe lost work as committed code.

Restored the original packaged workspace with its own bootstrap script, verified its receipt against the fetched lab/world HEAD, and ran `tidy check`: S0, S1, S2, S3 are 0.1.0 and pass unchanged-surface checks. The package includes a pre-existing package-lock.json change and bootstrap logs; these are not investigation changes and are excluded from commits.

Previously reported NorthPark S0–S3 counts (18 Badges, 17 Baskets, 16 visible Tees) and byte-identical repeat runs are retained in the contact packet. They are prior observations, not a fresh whole-corpus proof. Pathfinding and semantic completion remain UNKNOWN.

## Owner constraints carried forward

- Molecule: composition whose structure carries semantic meaning; not merely a UI component.
- PxC should retain useful pixels, objects, and measurements, with reusable semantic/statistical calculations. Warm work must not silently reuse stale code or inputs.
- New experiments live outside frozen clean/ surfaces. Leave the default computation intact; retain rejected alternatives and their receipts.
- A tee major-axis ray through its centroid should pass within 5 degrees of its badge. Treat violations first as possible measurement or annotation failures; do not manufacture tee identity from a useful angle. Record this as an owner-supplied diagnostic constraint until measured.
- A basket can completely cover the next tee. No visible remnant means no claimed visual recovery. Inferred endpoints must remain distinguishable from observed endpoints.
- Course variety makes median-distance assumptions suspect. Do not fit course geometry to an assumed typical hole.
- Understand alpha and antialiasing; subtract exact relevant owned pixels and inspect residue. Basket bounding boxes are not ownership masks.
- Inspect each course and failure crop visually. Record observations separately from candidate causal explanations.
- Keep regular decisions, falsifiers, outcomes, and remaining UNKNOWNs here, alongside reusable LAB commands and receipts.

## Next reachable work

1. Re-establish a current six-course S0–S3 baseline through LAB, with source/overlay inspection and machine-readable retained results.
2. Expose the already-existing S3 fill-consistent selector through an isolated experimental Stage adapter and compare against clean without rerunning upstream work unnecessarily.
3. Recover downstream pathfinding through existing production code. Do not equate command success, a count of 18, or zero paths outside bounds with semantic completion.

No promotion authorized by this checkpoint; this commit preserves custody and decisions, not a solved detector.
