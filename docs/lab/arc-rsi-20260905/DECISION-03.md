# ARC-RSI decision 03 — the referee and the occluder

## Corrections with consequences

The registered `lab test` referee has a LAB-owned source/crop reconciliation path. It can verify DashsTrack, Heritage, Lenard, and TowneLake against the attached archive without exposing Annotation coordinates. The earlier byte-only grader's five blocked sources are not the final available boundary. AlexClark and NorthPark remain outside this registered referee's declared scope.

HUH: the original candidate exporter unconditionally read `px.tees`. It graded the retained clean set inside an experimental snapshot. The corrected exporter honors the receipt's explicit selection address, refuses missing/conflicting selections, and now supports separate Basket/Tee selection addresses. No truth transformation, matching tolerance, or success rule changed. Five synthetic actual-export tests pass.

Corrected S3 interior-core referee: DashsTrack 18/18 Tees; Lenard 18/18; TowneLake 18/18; Heritage 15/18, missing H5/H6/H10. Basket checks still failed on DashsTrack (H6), Heritage (H12/H13/H17), and Lenard (H9/H12). These are semantic hole IDs from the public referee, not hidden coordinates.

## S2 active experiment

The old matched Basket sprite calculation contains useful knowledge. Generic bright-mask replay admitted roofs (22/18/20/29/20/20 detections across Dash/Alex/Heritage/Lenard/North/Towne). Rejected.

Learning the renderer body color from clean Basket-owned pixels removed roof confounders. Three separately merged discovery passes produced duplicate Heritage matches and still missed Lenard's Basket visibly covered by Badge 5. Rejected, implementation retained.

`S2/exp/sprite-visible-support` combines three coarse discovery sections in ONE matching pursuit and excludes exact Badge-owned pixels from the visible template denominator. Hidden cells are not negative observations. It does not infer an invisible Basket from no support.

All six source-level renderings were opened: 18 Basket poses each. Registered checks on actual fitted tips pass 18/18 for all four supported courses. This is spatial presence, not proof of endpoint assignment, complete ownership, or paths.

The probe is now an isolated real S2 Stage contract with six Ticks, PCR, warm S0-S1 reuse, and a separate `px.baskets.exp.objects` address. Clean `px.baskets` is preserved. Native runs reproduce 18 each. All ten newly admitted objects were opened in source/observed-B+W/subtraction crops. Every coincident original Basket pose is exactly aligned with its clean body bbox; original owned component pixels are preserved except for explicit prior Badge ownership. Hidden pixels are never manufactured. Antialiasing completeness remains UNKNOWN.

A visible nuance: Dash's added Basket is partly hidden by another Basket, not a Badge. Badge-hidden cell count alone must not mean fully observed. Keep missing observed body support distinct from the identity of its occluder.

Build passes. Current focused batch: 24 passing tests, including six new sprite/ownership tests and five actual referee-export tests. The initial declaration build failed because an exported inferred type referenced a private legacy interface; the explicit ReturnType annotation repaired that without changing the legacy module. Full suite is not yet established. No clean surface was edited; no Stage was promoted.

Local exact artifacts: `S2-VISIBLE-RUNS.json`, `/mnt/data/arc-rsi-sprite-visible-grades/grades.json`, and `/mnt/data/arc-rsi-basket-ownership-percepts.png` with snapshot sidecar. The new S2 implementation, selected-state referee, and rejected variants are still local pending the next source checkpoint; this note does not claim they are already on the remote branch.

Next: verify the real S2 contract through the referee, correct partial-visibility testimony, then compose its observed ownership with S3/S4 recovery without silently substituting any clean address. Alex's green-overlay fragment and Heritage's unresolved Tees are live questions. No pathfinding or whole-world acceptance claim.
