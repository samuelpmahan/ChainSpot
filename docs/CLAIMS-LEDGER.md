# The Claims Ledger

Every load-bearing diagnostic claim gets a row here WITH its receipt (the
evidence a human can check), and its fate. A retraction is a first-class
entry — the ledger exists because claims that sound like evidence get
believed, and the only defense is receipts visible to the owner plus a
public record of which claims died on contact with them.

Rules (binding on every agent working this repo):

1. A claim that steers work (a diagnosis, a root cause, a "this hole is
   missing because...") is entered here when made, status PENDING, with its
   receipt: verbatim receipt lines, artifact paths, or the command that
   reproduces the evidence. No receipt, no claim.
2. When a receipt survives challenge, mark UPHELD. When falsified, mark
   RETRACTED and record what falsified it and what the truth turned out to
   be — the retraction text is the valuable part.
3. Never edit a dead claim's row to look smarter; append the correction.
4. This file is a handoff document like INTAKE-ENGINE-HANDOFF.md: agents
   update it in the same commit as the work it describes.

Format: `date | claim | receipt | status | fate`.

---

## 2026-08-28 — founding entries (the day the ledger earned its existence)

| # | Claim | Receipt | Status | Fate |
|---|---|---|---|---|
| 1 | NorthPark H14's tee pad points ~19.8° off its badge ray; the 3° axis gate is what blocks recovery. | G4 rejection pixels (679-686,1026) + offline PCA vs ray math. | **RETRACTED** | The "shard" was badge 15's "5" digit glyph (14×21, area 155, centroid (683,1036) = H15's badge position). The angle was measured on chrome. Falsified by the owner's crop challenge + the component forensic table. Truth: digit glyphs escaped ownership subtraction (fixed, `dc96000`); H14's real pad likely merged with the basket glyph — reclassification pending. |
| 2 | NorthPark H16's badge baseline (~8px) makes the badge ray numeric noise; needs a short-baseline PCA fallback. | Component table: rejected shard = comp#304, 7×21, area 78, 8px from badge. | **RETRACTED** (evidence), reframed | comp#304 is badge 16's own "1" digit. The real pad is comp#306 (19×14, area 153, (798,1169), 5.0° on-axis) — never considered, because... see claim 3. And per the owner's z-order observation the pad renders ON TOP of its C2D, so H16 is a **G3 visible-detection defect** (dash-broken ring), not a recovery case at all. |
| 3 | G4 recovery discovery only searches ~83px around the PREDECESSOR hole's basket tip; everything else is structurally unfindable. | `g3.teeRecovery.ts:622-630` (anchor = basket tip, radius = hypot(padHalf)+observedSpan+4); post-fix NorthPark trace `seedFragments: 0, candidates: 0` with a 153px on-axis pad 60-100px away. | **UPHELD** | Owner's law issued (no absolute course-distance assumptions, ever). Fix in flight: predicate-as-filter over all unowned bright components, known-occluder pixel subtraction, no spatial prefilter. |
| 4 | Badge digit glyphs are separate bright components inside the plate's dark interior and escape recovery's ownership subtraction. | Component signature trio at every badge (plate ~450px 55×42, "1" digit 78px 7×21, second digit ~155px 14×21); pre-fix rejections vanish post-fix. | **UPHELD** | Fixed in `dc96000` (own every bright pixel in badge bbox). DashsTrack parity held byte-exact. |
| 5 | HeritagePark H5 has "no visible evidence" (complete-invisibility / phantom territory). | Absence of G4 hypotheses in the trace. | **RETRACTED** | comp#1096: 50px, 12×16, at (729,1154), 77px below badge 5 — real, visible, outside the discovery box. Falsified by the owner's crop + component table. Class reassignment pending the invariant classification. |
| 6 | HeritagePark H6's visible support is 2 pixels (< 8 minimum). | `rejectionReason badge 6: visible component support 2 < 8`. | **RETRACTED** (as stated) | The 2px was the anti-aliasing halo of badge 6's own digit — chrome again. The real pad is comp#778: 21px, 3×7, 71px below the badge, 4.0° on-axis, outside the discovery box. |
| 7 | Heritage H17's pad is absent from the bright mask near the badge (mask-threshold class). | Component table: only chrome + 2px specks within 110px of badge 17. | PENDING | The owner's crop shows a pad to the right, possibly beyond the inspected window or too dim for the mask. Needs the wider look; discovery lane's classification will rule. |
| 8 | Every tee is either non-occluded (G3 must find it) or occluded by a known occluder (G4 must recover it); no third state. | Owner invariant, 2026-08-28. | ADOPTED as contract | Discovery lane must classify every missed Dev6 hole: G3-defect / recovered / recovery-rejected(reason) / invisible — each with pixel receipts, entered here when delivered. |
| 9 | C1S/C2D range circles (solid 10m / dashed 20m) are in-scene renderer chrome whose z-order vs the tee pad varies by course zoom (NP: pad on top; DT: pad under), and their fitted pixel radii imply meters-per-pixel. | Owner observation + `g3.teeReceipts.ts:136` vocabulary; StripChrome receipt shows intake never touches them. | PENDING | Chrome classifier + metersPerPixel measurement chartered in the discovery lane; receipts to land here. |
| 10 | Axis-tolerance soft ceiling (`axisToleranceDeg` knob, G4 `teeRecovery`): once `dc96000` owns badge-interior chrome, does any REAL occluded tee shard need more than the strict 3° gate to recover? | Ladder (3/5/10/20/30/45/88°) via `./lab sweep` on NorthPark/HeritagePark/AlexClark/DashsTrack, plus a direct-engine per-hypothesis check (`runThreeFactor` on each sweep's own canonical raster) verifying every ACCEPTED fitted center against every badge bbox. Result: HeritagePark recovers H10, AlexClark recovers H10+H11, DashsTrack holds 18/18 (H3/H5/H12 via recovery) — recoveredTees/assignments/fitted-centers/axis-errors are IDENTICAL at every rung from 3° through 88°; NorthPark recovers 0 tees at every rung (real pads sit outside the discovery search bounds — claims 3/5, not an angle problem). No accepted center ever falls inside a badge bbox at any tolerance tested. Full table + per-hypothesis coordinates: `artifacts/orchestration/axis-ceiling-progress.md`. | **NULL RESULT (receipted)** | The soft ceiling buys nothing on the current corpus once chrome is owned (`dc96000`): the strict 3° TARGET already captures every hole the mechanism can capture. The knob is landed (`axisToleranceDeg` on `teeRecoveryFeature`, default **3**, validated 0.5-90, wired into `teeRecoveryUnit.run`, receipted via the `teeRecovery/axisErrorDeg` measurement) for a future corpus that genuinely needs slack, but the default was NOT moved off the target. Answers the "Axis-tolerance ladder" standing docket below. |

## 2026-08-28 — discovery de-footgun lane: settlements

| # | Claim | Receipt | Status | Fate |
|---|---|---|---|---|
| 10 | Row 3 root-cause, precise mechanism: on the real NorthPark canonical raster, H16's own tee pad (bright component 19x14/area 153 at (798,1169), componentLabel 306) DOES fall inside the old predecessor-basket radius box by coordinate math (distance to basket-9's tip = 57px < radius 88.59px) -- but it never reached `visibleComponents` because `ownership`, not the region test, excluded it: G3 had already registered that exact component as `tee-11`'s pad, and G5/G6 assignment/scoring had mis-paired `tee-11` to badge-4 (hole 12) at a near-zero garbage score (2.4e-13) instead of hole 16. | Instrumented trace on `dc96000` unmodified code, real NorthPark sweep: `near component label=306 ... distToAnchor=57.0 ... ownedCount=153 inBoxCount=153` (all 153 pixels owned) plus `tee tee-11 pad.bbox=[789,1162,19,14] pad.componentLabel=306` and `assignment badgeId=badge-4 teeId=tee-11 basketId=basket-5 score=2.428394544561205e-13`. | UPHELD, refines rows 1-2 | This is NOT "row 2's G3 z-order/dash-broken-ring defect" -- G3 correctly found and localized the pad as a normal, complete, non-occluded tee (`tee-11`). The defect is downstream, in G5/G6 assignment scoring accepting a 2.4e-13-score row over letting hole 16 claim its own visible tee. See row 12 below for what actually happened once the tee pool grew. |
| 11 | NorthPark H14 (badge 14, bbox [674,946,56,42]) has no bright component registered as a tee pad anywhere near its own badge; its predecessor's basket (basket-8, hole 13's basket, tip (919,1095)) sits ~245px from badge 14's position, far outside the ~89px predecessor-anchored box -- so the box design could never search where H14's evidence would be even if present. | Full badge/tee position dump from the same instrumented `dc96000` trace: no `tee.pad.bbox` entry falls within ~150px of badge 14's centroid (700.2,966.5); nearest is `tee-9` at (823,1067), 146px away. | UPHELD | Confirmed the genuine "predecessor-anchor is the wrong worldview" shape of the footgun (as opposed to H16's assignment-scoring shape). Fixed structurally by removing the spatial prefilter entirely (row 3's fate). |
| 12 | After replacing the predecessor-basket-radius box with a global, no-spatial-prefilter search (every unowned, non-occluded bright component is a candidate for every missing badge; strict predicate unchanged), NorthPark H14 and H18 each get a new accepted G4 recovery candidate, and the resulting larger tee pool lets G5/G6's whole-board reassignment also correctly re-pair `tee-11` to hole 16 (fixing row 10's mis-pairing as a side effect, without any code change in scoring/routing). All 18 NorthPark holes end with a real assignment and 0 warnings. | `packages/alg/src/detectors/threeFactor/features/g3.teeRecovery.ts` (`buildTeeRecoveryCandidates`, `git log` on this lane's commits); real NorthPark sweep after the fix: `results.recoveredTees: 2`, final table rows `H16 \| badge-10 \| tee-11 -> basket-10`, `H14 \| badge-7 \| tee-recovered-0 -> basket-5`, `H18 \| badge-12 \| tee-recovered-1 -> basket-13`, `WARNINGS (none)`. Repro: `./lab sweep packages/alg/src/detectors/threeFactor/configs/tee-recovery-on.json chainspot-corpus/dev/NorthPark/NorthPark-full.png`. | UPHELD | Resolves row 7's open PENDING adjacently (Heritage H17 still separately triaged below) and settles row 8's contract requirement for NorthPark: H16 classifies as **recovered-via-reassignment-cascade** (G3 found it correctly; G5/G6 scoring lost it; G4's bigger tee pool let reassignment recover it), not G3-defect and not a G4-recovery-direct-accept. H14 and H18 classify as **recovered** (direct G4 accept). See the discovery-lane final report for the full per-hole class table (also covering Heritage/AlexClark) and the DashsTrack full-parity receipt (18/18/18/18/4860 raw pairs/0 warnings, `tests/unit/dashsTrackSweep.test.ts` G6 now a real assertion, not `test.fails`). |
| 13 | C1S/C2D range-circle dash pixel-subtraction classifier (row 9) is designed but deferred out of this lane: the fix must be per-course, per-basket circle fit (never an assumed radius) with pixel-level (not whole-component) subtraction, because z-order between the tee pad and its C2D ring is not fixed across courses. Screen-chrome pixel subtraction (the already-built `screenChrome.ts` classifier) IS wired into this lane's discovery as a stopgap known-occluder cut. | Design note inline at `g3.teeRecovery.ts` (above `buildTeeRecoveryCandidates`); screen-chrome wiring: `exactKnownPixels`/`visibleComponents` in the same file, receipts via `ctx.overlay('teeRecovery', {... reason: "component N: K pixels subtracted as screen chrome ..."})` and `ctx.measure('teeRecovery','chromeSubtractedComponents', ...)`. | DEFERRED (documented) | Do not build features on this until the circle fit lands. `metersPerPixel` measurement (row 9's bonus ask) is not emitted by this lane for the same reason -- it depends on the same fit. |

## Standing dockets awaiting receipts

- Axis-tolerance ladder (soft ceiling, target P100 5° then 3°): does any
  REAL shard need more than the strict gate once chrome is owned and
  discovery is unbounded? Null result is a legitimate verdict.
- The 8 missed Dev6 holes, classified per the invariant (claim 8).
- Minesweeper Index HIGH items (docs/minesweeper/): each fix must enter
  here with its before/after receipt.

## 2026-08-28 — phantom budget + seven-whys additions

| # | Claim | Receipt | Status | Fate |
|---|---|---|---|---|
| 14 | AlexClark reaches 18/18 assignments with phantomTee enabled, spending exactly ONE phantom (owner mandate: one hole). | tee-recovery-phantom-on A/B on AlexClark: 13 visible + 5 recovered = 18 total, `phantomTee synthesized: 1, unresolved: 0`, 18 assignments, 0 warnings. Budget now ENFORCED: `phantomTee.maxCompletions` knob (default 1), holes beyond budget stay loudly unresolved; config pins maxCompletions: 1. | **UPHELD** | Owner policy encoded as a knob, not a hope. Follow-up docket: the HOLE ASSIGNMENTS table does not yet mark WHICH recovered row is the phantom (assignment-only, appearance UNKNOWN) — labeling gap for the next receipts pass. |
| 15 | The tee-11 mis-pairing (2.4e-13) has a systemic root cause: count-completeness metrics pay for silence. | Seven-whys analysis with per-level receipts and a 7-rung corrective ladder: docs/seven-whys/2026-08-28-tee11-mispairing.md. Rung 1 landed (`ae68617`); rungs 3-7 open (selection viability floor, effectively-unreachable verdict, receipt anomaly line, plausibility-as-receipt-content policy, viable/forced assignment split). | ADOPTED | Standing docket: implement rungs 3-7. |

## 2026-08-28 — owner-challenged corrections ("allergic to any proof I see")

| # | Claim | Receipt | Status | Fate |
|---|---|---|---|---|
| 16 | "AlexClark 18/18 assignments with one phantom" (row 14, and orchestrator chat claims). | The phantom-run HOLE ASSIGNMENTS table itself: 18 rows, but two are `H1868 | badge-10 | ... | 0.028` and `H295 | badge-16 | ... | 0.003` — garbage OCR labels. Labels 5 and 18 are absent from the table; the garbled badges are almost certainly the real H5/H18. | **CORRECTED** | AlexClark true coverage = **16 of 18 holes**; "18 assignments" was row count, not hole coverage. The phantom completed exactly one label-space hole (H12 — the only previously-missing integer label now present, as `tee-recovered-1`). The phantom did NOT and could not touch the OCR failure: a badge whose label is garbage is invisible to phantom's missing-hole computation entirely. Blocker owned by G1 OCR; strike team (Opus + 2 Sonnets) dispatched. Row 14's count claims inherit this correction. |
| 17 | "The hunt procedure went 4-for-4" (orchestrator chat claim). | Discovery lane's Dev6 table: NorthPark H14 recovered (direct), H16 recovered (theft diagnosis UPHELD, fixed via reassignment cascade), Heritage H6 recovered — 3 confirmed. Heritage H5: diagnosed as "true recovery case, comp#1096 50px visible" but the lane reports H5 STILL MISSED post-rebuild — the diagnosed component either failed strict acceptance or is not H5's tee. | **PARTIALLY RETRACTED** | 3 hunts upheld, 1 (Heritage H5) unresolved — the diagnosis outran its receipt. H5 re-enters the hunt queue under cv-tee-hunter with the acceptance-trace requirement: why was comp#1096 rejected or not candidate-matched to badge 5? |


## 2026-08-28 — G1 badge digit garbage (OCR strike team, Opus lead forensics)

| # | Claim | Receipt | Status | Fate |
|---|---|---|---|---|
| 18 | ROOT CAUSE of every G1 badge digit garbage read: `extractBadgeGlyph`'s frame-exclusion test `brightLabels[i] !== badge.label` (`packages/alg/src/detectors/threeFactor/digits/badgeGlyph.ts:55`) is a silent NO-OP for `dark-plate-recovery` badges, because `recoverDarkPlateBadges` stamps them with the sentinel `label: -1` (`packages/alg/src/detectors/threeFactor/badgeStage.ts:157`) and no pixel in `brightLabels` is ever -1. The badge's own bright plate OUTLINE is therefore fed to `segmentDigits` as if it were a glyph. Every observed failing badge is `source='dark-plate-recovery'`; no `bright-family` badge on any Dev6 course tested is affected. | `node scripts/ocr-intruder-probe.mjs <run>/renders/input/g0.canonical.png <out> 10 16 9` on AlexClark: badge-10 `label=-1 plateArea=1306`, glyph mask fed by component `217` bbox **57x40 spanning the whole 57x40 interior** plus the two real digits (`223` 7x21, `224` 14x21); healthy control badge-9 `label=141 plateArea=445`, fed by digits ONLY (`143` 7x21, `144` 14x21). Same shape on NorthPark badge-2 (`223` 55x40) and HeritagePark badges 7/9/12/14 (`1171` 55x43, `1254` 53x43, `1284` 51x43, `1299` 54x43), all `label=-1`. Visual: `artifacts/ocr-forensics/<Course>/badge-<n>.crop6x.png` (6x, magenta = pixels handed to segmentDigits) — `HeritagePark/badge-7.crop6x.png` shows a badge plainly reading **12** with the plate outline tinted magenta alongside the digits. Full analysis: `docs/seven-whys/g1-badge-digit-garbage.md`. | **UPHELD** (proved by repair, see row 19) | Fix contract C1 issued to the builders: exclude the plate frame for EVERY badge source; the `-1` sentinel comparison must not survive. Detector source untouched by this lane. |
| 19 | The root cause is proved by repair: excluding the plate-frame component from the glyph mask — changing NOTHING else — repairs all 7 observed failures and alters ZERO healthy reads. Recovered labels are exactly the labels absent from each course's confidently-read badges, and all three courses end complete 1-18 and collision-free. | `node scripts/ocr-counterfactual.mjs <run>/renders/input/g0.canonical.png <Course>`. HeritagePark: badge-7 `"62"@0.0262 -> "12"@0.9926`, badge-9 `"62"@0.0250 -> "13"@0.9891`, badge-12 `"17"@0.0044 -> "15"@0.9899`, badge-14 `"03"@0.0756 -> "2"@0.9926` (absent set was exactly {2,12,13,15}). AlexClark: badge-10 `"1868"@0.0278 -> "18"@0.9811`, badge-16 `"295"@0.0029 -> "5"@0.9892` (absent set exactly {5,18} — confirms the owner's hypothesis). NorthPark: badge-2 `"787"@0.0015 -> "7"@0.9936` (absent set exactly {7}). All 47 `bright-family` reads byte-identical before and after. | **UPHELD** | The 85%-interior-span rule used by the probe is a PROBE, not the prescribed fix — C1 requires positive identification of the frame from the plate geometry `recoverDarkPlateBadges` already measured, with printed provenance (footgun law). Set-completion is how this investigation corroborated its answer and must NEVER become how the detector picks a label (contract C5). |
| 20 | The 1-18 hole-label cap in `measure.ts labelCandidates` is enforced ONLY when the digit count is already 1 or 2 — i.e. only when it was never needed. `labelCandidates` (`measure.ts:164-181`) filters 1-18 to labels whose STRING LENGTH equals `reading.digits.length`; at 3+ digits that set is empty, and `measure.ts:250`'s `candidates[0] ? ... : entry.reading.label \|\| null` falls through and emits the raw uncapped concatenation from `readBadges.ts:96`. Whenever segmentation is wrong, the cap turns itself off. | Verbatim from `node scripts/ocr-forensics.mjs`, AlexClark badge-10: `labelCandidates(1-18, len==4) -> 0 candidate(s)  << EMPTY: cap bypassed, raw label falls through` / `EMITTED BadgeEvidence.label = "1868"`. Same on badge-16 (`len==3`, `"295"`) and NorthPark badge-2 (`len==3`, `"787"`). | **UPHELD** | Answers the owner's standing question ("the 1-18 cap lives in labelCandidates yet raw labels like 1868 still surface"). Fix contract C3: delete the `\|\| entry.reading.label` fallback; empty candidates means UNREAD (`null`), never the raw string. Distinct from the separate minesweeper HIGH item of WIDENING the 1-18 vocabulary — do not conflate. |
| 21 | A SECOND, more dangerous failure mode exists on HeritagePark: when the intruding plate outline is ~2x the digits' height, `segmentDigits`' relative height filter (`segment.ts:227-229`, `heightRatioMin` 0.5) DROPS THE REAL DIGITS AS NOISE and keeps only the outline's two valley-split halves. The digit count is then exactly 2, so the 1-18 filter DOES apply — and picks the argmax over pure noise, emitting a well-formed, in-vocabulary, entirely fabricated hole number. This is silent corruption: the label carries no signal that it is garbage. | `scripts/ocr-forensics.mjs` on HeritagePark, badge-7 verbatim: `note: dropped label=2 height=21 < 21.50 (0.5 x tallest=43)` / `note: dropped label=3 height=21 < 21.50 (0.5 x tallest=43)` / `note: label=1 bbox=55x43: valley-split at col=16 (colSum=6)` / `raw reading.label = "62"` / `labelCandidates(1-18, len==2) -> 9 candidate(s): 12@0.2033 16@0.1629 17@0.1293` / `EMITTED BadgeEvidence.label = "12"`. badge-14 emits raw `"03"` — a leading zero, no such hole — silently laundered into `13`. Collisions shipped with zero warnings: badge-7 and badge-9 BOTH `"12"`; badge-12 `"17"`@0.0044 collides with badge-17's genuine `"17"`@0.9927. | **UPHELD** | The height filter's stated reasoning (`segment.ts:20-25`, "a sub-half-height blob is not a digit") is sound only when the intruder is SMALLER than the digits; when it is twice their height the rule inverts and drops signal to keep noise. Fix contract C2 (reject >2 digits and leading zeros rather than emit/launder them), C4 (derived confidence floor + named abstention), C5 (collisions receipt-visible). |
| 22 | WTF-HAPPENED (regression vs `old-stuff`): the rebuild swapped a whole-label TEMPLATE MATCHER for a per-digit classifier with free-form concatenation, and dropped three safety properties the old design got structurally for free — (a) VOCABULARY was structural (`badgeGlyphClassifier.ts:355-357` ranks against the 18 rasterized `hole-01..18.png` templates, `:87-96` enforcing the filename contract; emitting `"1868"` was UNREPRESENTABLE), (b) ABSTENTION was first-class (`:31-35` reason enum, `:58-65` `minScore 0.58`/`minMargin 0.045`, `:363-364` decision, `:368` label set ONLY when abstention is null while `:369` keeps `bestLabel`), (c) COLLISION DETECTION existed (`:420-426` `badgeGlyphBatchIsComplete` requires all labelled AND `new Set(labels).size === labels.length`, used as the escalation gate at `sourceBadgeIdentity.ts:87`). The new pipeline has none of the three. | Both sides read in full at `ffcf9dc`: `old-stuff/src/lib/autoAnnotation/badgeGlyphClassifier.ts` (426 lines), `old-stuff/src/lib/autoAnnotation/sourceBadgeIdentity.ts:87-102`, `old-stuff/tests/unit/badgeGlyphClassifier.test.ts:125-133` (locks the empty-glyph abstention); vs `packages/alg/src/detectors/threeFactor/digits/readBadges.ts:96` (unbounded join, always emits) and `measure.ts:250` (no uniqueness check anywhere). All 7 failing badges scored 0.0015-0.0756 vs the 47 healthy at 0.978-0.994 — **the old classifier's `minMargin 0.045`/`minScore 0.58` would have abstained on all seven.** The information needed to reject was computed and then discarded. | **UPHELD** | Not a porting mistake: the SUBSTRATE changed from "choose one of 18 templates, or abstain" to "read digits, concatenate", and the three guarantees the first substrate provided for free were never re-established as explicit code in the second. The old file's last three public surfaces (abstention, `bestLabel`, `badgeGlyphBatchIsComplete`) are a checklist of what the new pipeline still owes. What the new design does BETTER and must keep: per-digit training generalizes past 18 labels, full per-digit evidence retained (`readBadges.ts:32-40`), and `labelCandidates` yields a ranked posterior rather than a bare winner. Fix contract C1-C6 + receipt format: `docs/seven-whys/g1-badge-digit-garbage.md`. |
## 2026-08-28 — G1 OCR fix contract landed (Sonnet builder)

| # | Claim | Receipt | Status | Fate |
|---|---|---|---|---|
| 25 | The G1 OCR fix contract (C1-C6, `docs/seven-whys/g1-badge-digit-garbage.md`) is implemented and repairs every known garbage read with zero healthy-read regressions, INCLUDING a previously-undetected 8th/9th failure the eval harness surfaced mid-build: Lenard badge-3 raw `"17"@0.0086` (collided with badge-15's genuine `"17"@0.9927`) and badge-9 raw `"2912"@0.0334` (4-digit, out-of-vocab) — both silently dropped H5/H12 from Lenard's coverage exactly like the original 7. C1: `extractBadgeGlyph` now takes an optional `PlateFrameGeometry` (badgeStage's already-measured `plateBbox` inset by the existing `plateInteriorMargin` knob) and positively excludes any bright component reaching all 4 edges of that measured rectangle (new `plateFrameTolerancePx` knob, default 2px, raster-geometry provenance, NOT the probe's 85%-span heuristic). C2: `classifyReading` structurally rejects digitCount>2 (`too-many-digits`) and a leading `'0'` (`leading-zero`) BEFORE any confidence path, unconditionally. C3: the `\|\| entry.reading.label` fallback is deleted; empty `labelCandidates` now always yields `label: null`. C4: a derived floor (`median(this run's finite margins) / confidenceFloorDivisor`, default divisor 8) plus a `labelAmbiguityMargin` (0.045, inherited from old-stuff's `minMargin`) drive named abstention (`empty-glyph`/`low-score`/`ambiguous`/`too-many-digits`/`leading-zero`/`collision`) on `BadgeEvidence.abstentionReason`, with `bestLabel` retained and a separate `fillFraction` field replacing the `darkFraction`-into-`confidence` overload. C5: new `resolveBadgeCollisions` restores `badgeGlyphBatchIsComplete`'s uniqueness — colliding labels keep the higher-confidence claimant, both parties get `conflictWith` populated, never resolved by elimination. C6: `BadgeReading.notes`/`BadgeEvidence.notes` forward `segmentDigits`' dropped-blob/valley-split lines end to end. | **Repro (per-badge, `node scripts/ocr-forensics.mjs <g0.canonical.png> <Course>` before vs. after, byte-diffed against a captured pre-fix baseline in `artifacts/orchestration/{baseline,postfix}/<Course>.txt`):** AlexClark badge-10 `"1868"@0.0278 -> "18"@0.9811`, badge-16 `"295"@0.0029 -> "5"@0.9892`; NorthPark badge-2 `"787"@0.0015 -> "7"@0.9936`; HeritagePark badge-7 `"62"@0.0262 -> "12"@0.9926`, badge-9 `"62"@0.0250 -> "13"@0.9891`, badge-12 `"17"@0.0044 -> "15"@0.9899`, badge-14 `"03"@0.0756 -> "2"@0.9926`; Lenard badge-3 `"17"@0.0086 -> "5"@0.9899`, badge-9 `"2912"@0.0334 -> "12"@0.9926`. All 9 repairs exact-match each course's set-completion prediction (the label absent from that course's confidently-read badges). Diffing full forensics dumps line-for-line: every one of the 47 originally-cited healthy `bright-family` reads plus Lenard's other 16 healthy reads are BYTE-IDENTICAL pre/post (diff shows ONLY the repaired badges' lines changed); DashsTrack and TowneLake (0 garbage badges each) are byte-identical in full. Full-pipeline (`./lab sweep .../default.json <course> `) distinct-hole coverage, computed by holding G2-G6's assignment rows constant (badge label is attached only at receipt time by `withHoleLabels()` and never feeds tee/basket geometry) and substituting the pre-fix vs. post-fix label per `badge-N`: HeritagePark 15/18 -> 17/18 (H5 still missing — a separate, pre-existing G3/G4 tee-recovery gap, ledger rows 5/17, untouched by this fix); AlexClark 15/18 -> 17/18 under the default (phantom-off) config (H12 still missing — the same known G3/G4 gap ledger row 16 describes); NorthPark 17/18 -> 18/18; Lenard measured directly via `tests/unit/corpusSweep.test.ts`'s real scoreboards: G1 16/18 -> 18/18 digits matched, G4 `assignedExact` 16/18 -> 18/18, 0 misses (G3 tee-position gap at H3 is separate and untouched). DashsTrack (`tests/unit/dashsTrackSweep.test.ts`) and TowneLake hold 18/18 byte-identical, as required. `npm run check`: 0 errors. Full unit suite: pre-existing timeout flakes only (`familyDeviationSweep`/`familyTuning`, present in the pre-fix baseline run too, unrelated to this lane); `tests/unit/corpusSweep.test.ts`'s two Lenard `test.fails` (documenting the exact pre-fix garbage this contract removes) and `tests/unit/exec.compile.test.ts`'s frozen-plan-fingerprint pin and `tests/unit/threeFactorConfig.test.ts`'s resolved-config-hash pin were consciously updated (new `plateFrameTolerancePx`/`confidenceFloorDivisor`/`labelAmbiguityMargin` knobs move both hashes; the schema pin (`tests/unit/threeFactorSchema.test.ts`) was regenerated the same way). New unit coverage: `tests/unit/g1DigitReading.test.ts` (13 tests, one per contract clause: fabricated dark-plate frame exclusion with and without the fix, C6 note-forwarding, C2 too-many-digits/leading-zero, C3 fallback-deletion, C4 floor/ambiguous abstention, C5 two- and three-way collision resolution). | **UPHELD** | Fixes: `packages/alg/src/detectors/threeFactor/digits/badgeGlyph.ts` (C1), `.../digits/readBadges.ts` (glyph/plate-geometry threading + `notes` passthrough, C1/C6), `.../digits/segment.ts` (knobs bundle, unused by segment.ts itself, same precedent as digitW/digitH), `.../badgeStage.ts` + `.../features/g1.badges.ts` (`plateFrameTolerancePx` knob + `BadgeStageResult` provenance fields), `.../features/g1.digits.ts` (`confidenceFloorDivisor`/`labelAmbiguityMargin` knobs), `.../measure.ts` (`classifyReading`, `resolveBadgeCollisions`, rewritten `makeBadges`), `.../types.ts` (`BadgeEvidence` C2-C6 fields + `BadgeAbstentionReason`), `packages/alg/src/exec/operations.ts` (thread the two new plate-geometry fields into its own `BadgeStageResult` construction). Territory respected: no `./lab digits` command added, `scripts/chainspot-lab` untouched. Follow-up (out of this lane's territory, unrelated to OCR): Heritage H5 and AlexClark H12 remain open G3/G4 tee-recovery items (ledger rows 5/16/17); the G1 DIGIT READS receipt-block rendering (forensics doc's "RECEIPT FORMAT" section) is data-ready on `BadgeEvidence` (rawLabel/digitCount/bestLabel/abstentionReason/confidenceFloor/conflictWith/notes all populated) but no renderer consumes it yet — left for the sibling `./lab digits` scoreboard lane per this lane's territory boundary. |

| 26 | Post-merge (row 25's fix + row 24's `./lab digits` harness, same worktree): the harness's own corpus fixture (`tests/unit/digitsScoreboardCorpus.test.ts`, row 24) hard-coded the pre-fix garbage it was built to reproduce and broke once the fix landed — consciously updated to assert the repaired reads instead of the defect. `./lab digits all` (row 24's own measurement instrument) is now the single end-to-end acceptance gate for this whole lane. | `tests/unit/digitsScoreboardCorpus.test.ts` updated: badge-10 now asserted `"18"@~0.981` (was `"1868"@~0.028`), badge-16 `"5"@~0.989` (was `"295"@~0.003`), and ALL 18 AlexClark verdicts OK (was 16 OK + 2 GARBAGE-LABEL); test passes. `./lab digits all` verbatim, post-merge: `DashsTrack SUMMARY — 18 badges, 18 ok, 0 garbage-label, 0 low-confidence, 0 collision, 0 unread` / `Lenard SUMMARY — 18 badges, 18 ok, ...` / `TowneLake SUMMARY — 18 badges, 18 ok, ...` / `NorthPark SUMMARY — 18 badges, 18 ok, ...` / `HeritagePark SUMMARY — 18 badges, 18 ok, ...` / `AlexClark SUMMARY — 18 badges, 18 ok, ...` / `DIGITS — 6 course(s) scored, all OK`, exit code 0. `npm run check`: 0 errors (612 files). Full unit suite post-merge: only the 3 pre-existing timeout flakes (`familyDeviationSweep`/`familyTuning`, present before any OCR work started) remain non-passing. | **UPHELD** | Every Dev6 course now reports zero defects through the harness's own truth-free instrument — the goal state row 24 was measuring against. |

## 2026-08-28 — OCR strike team: legacy-classifier resurrection (Sonnet B)

| # | Claim | Receipt | Status | Fate |
|---|---|---|---|---|
| 23 | The PRE-REBUILD pure-TS badge glyph classifier (`old-stuff/src/lib/autoAnnotation/badgeGlyphClassifier.ts`) reads every one of CURRENT's failing/garbage/duplicate badge reads correctly, and does so by a fundamentally different, more failure-resistant mechanism: whole-glyph Dice-coefficient template matching against a FIXED 1..18 vocabulary with margin-gated abstention, vs. CURRENT's connected-component digit *segmentation* + independent per-digit logistic classification with no global vocabulary or bijection constraint. | Standalone offline port (`scripts/chainspot-lab/legacyBadgeClassifier/oldClassifier.ts`, verbatim logic, only fetch/canvas template-loading shimmed to pngjs/fs; self-test 18/18 canonical templates classify as themselves, matching `old-stuff/tests/unit/badgeGlyphClassifier.test.ts`'s own contract). Full Dev6 sweep (108 badges, `artifacts/orchestration/legacy-badge-classifier/head-to-head.json`): OLD's best-guess label (including its top candidate on an abstained/ambiguous read) forms an **exact bijection onto 1..18 on all 6 Dev6 courses**, even where CURRENT emits duplicate labels (Lenard "17"x2; HeritagePark "12"x2 and "17"x2) or pure-garbage multi-digit strings (AlexClark badge-10 "1868"@0.028, badge-16 "295"@0.003; NorthPark badge-2 "787"@0.002; Lenard badge-9 "2912"@0.033). Every disputed badge in the named failure set was visually confirmed against the actual crop pixels (`artifacts/orchestration/legacy-badge-classifier/disputed-badges-contact-sheet.png`): AlexClark-10 crop reads "18" (OLD abstained-best-guess 18@0.88, correct; CURRENT "1868"); AlexClark-16 reads "5" (OLD 5@0.82 confident-correct; CURRENT "295"); NorthPark-2 reads "7" (OLD 7@0.93 confident-correct; CURRENT "787"); HeritagePark's dup-"12" pair resolves to real values 12 (badge-7, both agree) and 13 (badge-9, OLD abstained-best-guess 13@0.79 correct vs CURRENT's colliding "12"@0.025); HeritagePark's dup-"17" pair resolves to real values 17 (badge-17, both agree) and 15 (badge-12, OLD abstained-best-guess 15@0.78 correct vs CURRENT's colliding "17"@0.004); a third HeritagePark misread found along the way, badge-14, reads "2" (OLD 2@0.91 confident-correct vs CURRENT's "13"@0.076). OLD was wrong in zero of the examined cases. | **UPHELD** | Regression theory holds with receipts: CURRENT's digit-segmentation architecture (`segment.ts`'s connected-component + wide-split + per-digit logistic path) is the mechanism at fault, not badge localization (both classifiers ran on CURRENT's own `detectBadges` bbox/center geometry). Fix ownership is the Opus-lead contract, not this lane; this row is evidence handoff only. Standing docket: does a whole-glyph-template or vocabulary-constrained-decode strategy belong in the fix, given the old mechanism's near-perfect empirical track record on this exact corpus? |

## Standing dockets (OCR lane)

- Whole-glyph template matching / fixed-vocabulary decoding as a candidate
  ingredient for the digit-OCR fix (row 16) — the fix contract itself is
  owned elsewhere; this is an input to that decision, not a claim on how to
  implement it.
- OLD's pure-ts abstention rate on Dev6 (18/108 badges, ~17%) trades recall
  for zero observed false positives; whether CURRENT's fix should adopt an
  analogous abstain-on-ambiguity gate (vs. always emitting a label) is
  unresolved.
## 2026-08-28 — OCR strike team: the digit-read scoreboard (harness lane)

| # | Claim | Receipt | Status | Fate |
|---|---|---|---|---|
| 24 | A new, read-only `./lab digits` command (`scripts/chainspot-lab/scoreboard/{verdict.ts,digitsCli.ts}`) reproduces every known live Dev6 digit-read defect via ONLY the fast public `detectBadges` seam (G1 badge stage + digit reading; no baskets/tees/support field/assignment; no Annotation truth consulted anywhere) and gates on it (nonzero exit if any badge is non-OK). | Verbatim `./lab digits all` output: `DashsTrack SUMMARY — 18 badges, 18 ok, 0 garbage-label, 0 low-confidence, 0 collision, 0 unread` / `Lenard SUMMARY — 18 badges, 15 ok, 1 garbage-label, 0 low-confidence, 2 collision, 0 unread` / `TowneLake SUMMARY — 18 badges, 18 ok, 0 garbage-label, 0 low-confidence, 0 collision, 0 unread` / `NorthPark SUMMARY — 18 badges, 17 ok, 1 garbage-label, 0 low-confidence, 0 collision, 0 unread` (`badge-2 \| 787 \| 0.002 \| ... \| GARBAGE-LABEL`) / `HeritagePark SUMMARY — 18 badges, 13 ok, 0 garbage-label, 1 low-confidence, 4 collision, 0 unread` (`badge-7\|12\|0.026\|...\|COLLISION (also badge-9)`, `badge-9\|12\|0.025\|...\|COLLISION (also badge-7)`, `badge-12\|17\|0.004\|...\|COLLISION (also badge-17)`, `badge-17\|17\|0.993\|...\|COLLISION (also badge-12)`, `badge-14\|13\|0.076\|...\|LOW-CONFIDENCE`) / `AlexClark SUMMARY — 18 badges, 16 ok, 2 garbage-label, 0 low-confidence, 0 collision, 0 unread` (`badge-10\|1868\|0.028\|...\|GARBAGE-LABEL`, `badge-16\|295\|0.003\|...\|GARBAGE-LABEL`) — exit code 1 overall. `./lab digits AlexClark --crops` writes a labeled two-tile contact sheet (`artifacts/digits/AlexClark-failures.png`) showing exactly the badge-10 and badge-16 crops. Corpus-dependent acceptance test `tests/unit/digitsScoreboardCorpus.test.ts` pins the AlexClark fixture (skips cleanly without corpus); pure classification is unit-tested with fabricated readings in `tests/unit/digitsVerdict.test.ts` (17 tests, all passing, no corpus dependency). `npm run check`: 0 errors. | **UPHELD** | This is the measurement instrument for the OCR strike team's fix lane: any fix to badge digit reading is proven or disproven by `./lab digits all` moving these exact counts toward 18 ok / 0 defects per course, never by narrative. Constraint held: no edits to `packages/alg` detector source or `digits/`; `detectBadges` used strictly as the read-only seam it already is. |


## 2026-08-28 — Heritage H5 root cause + the phantom-budget arithmetic (owner's three-part ask)

| # | Claim | Receipt | Status | Fate |
|---|---|---|---|---|
| 27 | Heritage H5's tee was NEVER hunted by G4 recovery — the hunt's missing-badge set is computed from the PRE-recovery G6 assignment, and at that moment badge 5 (H5) was not missing: it held a mis-paired tee. Pre-recovery, badge-5 was assigned `tee-12` at score 0.189 **rank 1** — but `tee-12` sits at canonical (708,1394), **52px from H4's badge (718,1343) and 317px from H5's badge (721,1078)**: it is physically H4's pad. The hunt therefore visited exactly the pre-recovery missing set {6,10,15,18} and never looked at H5. The post-recovery global re-assignment then corrected `tee-12` onto H4 (score 0.366) — exposing H5 as the unmatched badge — but recovery had already run. A mis-pairing MASKED a missing tee from the very machinery built to recover it. H5's pad evidence was there all along: unowned bright component #1096 (50px, 12x16, at (729,1154), 77px down-ray of badge-5) — 6x the 8px MIN_SHARD_SUPPORT, larger than the 21px fragment that successfully recovered H6 — sitting beside basket glyph #1051 (1746px, 42x66: the standard sprite signature, i.e. the known occluder that broke the pad's ring for G3). Classification per cv-tee-hunter: class (b) occluded-recoverable, failed by a NEW failure mode "masked from the hunt", not by the predicate. | Fresh `./lab sweep default.json HeritagePark-full.png` (post-OCR-fix, planFingerprint 1649c2b1...): `artifacts/sweep/dev72-recovered-default/HeritagePark-full/`. Pre-recovery table (`artifacts/measurementTable/assignment.selection.table.bin`, 14 rows): `H5 badge-5 tee-12 -> basket-1 score=0.189482 rank=1`; missing pre-recovery = {6,10,15,18}. Receipt rejection lines mention ONLY `badge 10:`/`badge 15:`/`badge 18:`/`badge 6:` (10 each, `grep -o "badge [0-9]*:" run.receipt.txt \| sort \| uniq -c`) — zero `badge 5` lines in text or machine receipt. Final HOLE ASSIGNMENTS: `H4 badge-8 tee-12 -> basket-6 0.366 rank 1`, H5 absent (17 rows). Geometry: badge centers from `./lab scope holes` (truth-free digit-derived); tee-12 position/area from `tees.exclusion.kept.bin`; components near badge-5 from `badgeStage.components.bright.bin` (#1003 448px 54x42 = plate, #1013 155px 14x21 = digit, #1051 1746px 42x66 = basket sprite, #1096 50px = pad remnant). | **UPHELD** | This is the same failure family as the tee-11 2.4e-13 theft (docs/seven-whys/2026-08-28-tee11-mispairing.md) wearing a new coat: there the theft stole a DETECTED tee; here the theft HID an undetected one. Corrective ladder rungs 3-7 gain a rung 8: **recompute the missing-badge set after re-assignment and re-hunt (iterate recovery→assignment to fixpoint), or geometric-sanity-floor the pre-recovery pairings before deriving the missing set** (a rank-1 score of 0.189 at 317px passed today; the 745px H10 `tee-13` pairing at score 0 rank 88 in the same final table is the same smell). No detector change made in this lane — analysis only, owner decides the rung. |
| 28 | The phantom budget of one in exactly one course is not policy taste — it is ARITHMETIC: across all six Dev6 courses, exactly ONE hole has zero pixel evidence of its tee, and it is AlexClark H12. Post-OCR-fix fresh sweeps: DashsTrack, Lenard, TowneLake, NorthPark all 18/18; Heritage 17/18 where H5's pad evidence EXISTS (row 27 — recoverable, must not consume a phantom); AlexClark 17/18 (13 visible + 4 recovered) where H12's scope render shows only path, tree canopy, and the C2D dashed ring near badge 12 — no white pad pixels in any forensic panel. maxCompletions=1 is therefore exactly the number of truly invisible pads in the corpus; a run wanting a second phantom has a detection problem by construction. | Fresh post-fix receipts: `dev72-recovered-default/AlexClark-full/run.receipt.txt` — 17 assignments, all labels real hole numbers (H1868→H18, H295→H5 healed), H12 the only absent row; `tee-recovery-phantom-on/AlexClark-full/run.receipt.txt` — `synthesized: n=1 min=1 max=1`, `unresolved: ... max=0`, `results.phantomTees: 1`, `results.assignments: 18`, and the phantom row is `H12 badge-1 tee-recovered-1 -> basket-0`. Visual: `artifacts/scope/AlexClark-full/AlexClark-h12.png` (badge-1 read "12"@0.993, center (535,156) canonical). | **UPHELD** | The budget knob's note ("scalpel, not a spray") now has its measured justification. If a future course adds a second genuinely invisible pad, raising the budget is a per-course A/B config decision with this same evidence bar: scope render showing no pad pixels + a default-config receipt showing every other hole accounted for. |

## 2026-08-28 — Geometric hunt + plausibility lane (owner directive: "step-4 input must never come from step 6")

| # | Claim | Receipt | Status | Fate |
|---|---|---|---|---|
| 29 | The lane landed four structural fixes with receipt-named drop-outs everywhere, and its honest scoreboard is MIXED — pushed as work-in-progress, not victory. Fixes: (1) hunted-badge set is geometric (`deriveHuntTargets`: greedy badge<->tee claims, course-derived bound = median claim distance x `padClaimOutlierFactor` knob on g4.search, default 3) and the solver may only WIDEN it (badges with no surviving pre-recovery row), never shrink it; (2) recovered tees are bound to the badge whose predicate accepted them (`RecoveryProvenance.badgeId`) and assignment refuses cross-badge pairing; (3) `pruneImplausiblePairs` removes pairings beyond the claim bound before selection in BOTH exec paths, one named receipt line per dropped (badge,tee); (4) dup-suppression and redundant-recovery discard now name whose hunt died, against which tee, at what distance. Scoreboard: DashsTrack --through G4 now 18/18 TRUTHFUL (the prior 18 included H5 paired at 400-700px score 0.06 — a fake row the prune killed and the widened hunt replaced with its real recovered pad); TowneLake 18/18, AlexClark 17/18 unchanged; Heritage 16/18 where H4 and H18 now hold their CORRECT pads (prior 17 included the 745px score-0 H10 row); NorthPark 17/18 (H5 orphaned by the post-discard re-solve, a REGRESSION); Lenard 17/18 vs ground truth (H3:no-assignment, a REGRESSION, diagnosis interrupted). | Receipts: `artifacts/test/lab-sweep-through-g4/run.receipt.txt` (prune lines verbatim: "badge 5: pairing with tee-12 removed before selection (tee-badge distance 706.9px exceeds the course-derived claim bound 375.3px = median 125.1px x 3)"); `artifacts/sweep/dev72-recovered-default/{HeritagePark,NorthPark,TowneLake,AlexClark}-full/run.receipt.txt`; `[Lenard][G4] assignedExact=17/18 misses=[H3:no-assignment]` from `tests/unit/corpusSweep.test.ts`. Tests: `tests/unit/g4HuntTargets.test.ts` (4 tests), all recovery/receipt/pin suites green (43 + 38), `npm run check` 0 errors. Plan fingerprint a60669eb..., config hash 4421f931..., consciously re-pinned. | **PARTIAL** (structural fixes UPHELD; two course regressions OPEN) | The receipts exposed two deeper pre-existing defects, now the next rungs: (rung A) the PROMISCUOUS PREDICATE — one Heritage fragment at (277,910) satisfied the strict fit for badges 6, 5 AND 10 (one won at score 0.028, two dup-suppressed at ~1px); with no spatial prefilter a small fragment fits a 3°-aligned pose toward almost any badge, so acceptance needs a shape/support floor or a multi-badge-satisfiability rejection signal; (rung B) the solver has no COVERAGE-FIRST objective — every global re-solve can orphan a badge whose plausible tee sits unused (NorthPark H5, likely Lenard H3). Owner decides: fix forward on rungs A/B, or park the lane. `--through G4` still drags `assignment.selection` (G6) in as a prerequisite — true G4-before-G6 decoupling waits on rung B. Standing docket: receipt verbosity levels (confirm vs --dig). |
