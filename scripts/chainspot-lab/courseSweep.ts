/**
 * Compatibility entry point.
 *
 * LAB execution has exactly one implementation path: courseLab.ts. Keep this
 * filename so old prompts/scripts do not fork execution semantics or bypass
 * the dependency-aware stage cache.
 */
import './courseLab';
