name: KERCHOOOOO v7 probe

on:
  push:
    branches:
      - perf/kerchooo-v7-probe

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: perf/kerchooo-v7-probe
          path: ChainSpot

      - uses: actions/checkout@v4
        with:
          repository: samuelpmahan/chainspot-corpus
          path: chainspot-corpus
          lfs: true

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install and V5 baseline
        working-directory: ChainSpot
        run: |
          set -euo pipefail
          npm ci
          ./lab setup
          rm -rf artifacts/sweep
          /usr/bin/time -f '%e' -o /tmp/v5.wall ./lab sweep batch --through G4 packages/alg/src/detectors/threeFactor/configs/default.json NorthPark
          root=artifacts/sweep/dev72-recovered-default/batches/NorthPark/full
          sha256sum "$root/renders/run/run.visual.png" | awk '{print $1}' >/tmp/v5.visual.sha
          sha256sum "$root/artifacts/measurementTable/assignment.selection.table.bin" | awk '{print $1}' >/tmp/v5.assignment.sha
          cp "$root/run.receipt.txt" /tmp/v5.receipt.txt
          echo "V5_WALL=$(cat /tmp/v5.wall)s"
          grep -E 'supportField \||teeRecovery \||teeRecovery.shardDiscovery' /tmp/v5.receipt.txt || true

      - name: Apply V7 rebuild and test
        working-directory: ChainSpot
        run: |
          set -euo pipefail
          python3 .github/kerchooo_v7_patch.py
          npm run build --workspace=@chainspot/alg
          npm run check
          npx vitest run tests/unit/teeRecoveryFeature.test.ts

      - name: V7 NorthPark exact final parity
        working-directory: ChainSpot
        run: |
          set -euo pipefail
          rm -rf artifacts/sweep
          /usr/bin/time -f '%e' -o /tmp/v7.wall ./lab sweep batch --through G4 packages/alg/src/detectors/threeFactor/configs/default.json NorthPark
          root=artifacts/sweep/dev72-recovered-default/batches/NorthPark/full
          sha256sum "$root/renders/run/run.visual.png" | awk '{print $1}' >/tmp/v7.visual.sha
          sha256sum "$root/artifacts/measurementTable/assignment.selection.table.bin" | awk '{print $1}' >/tmp/v7.assignment.sha
          diff -u /tmp/v5.visual.sha /tmp/v7.visual.sha
          diff -u /tmp/v5.assignment.sha /tmp/v7.assignment.sha
          python3 - <<'PY'
          base=float(open('/tmp/v5.wall').read())
          opt=float(open('/tmp/v7.wall').read())
          print(f'KERCHOOOOO V7 probe: {base:.2f}s -> {opt:.2f}s = {base/opt:.2f}x, {(1-opt/base)*100:.1f}% faster than V5')
          PY
          grep -E 'supportField \||teeRecovery \||teeRecovery.shardDiscovery' "$root/run.receipt.txt" || true
