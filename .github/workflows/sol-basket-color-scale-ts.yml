name: Sol basket color scale TS check

on:
  push:
    branches:
      - sol/basket-color-scale-ts
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: npx tsx scripts/cv-probes/basket_color_scale_ts_smoke.ts
      - run: npm run check
