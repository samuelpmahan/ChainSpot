{
	"schema": "threeFactor-config@1",
	"name": "dev72-recovered-default",
	"note": "Frozen production baseline: G2 uses the surviving two-pass renderer-family basket detector; G3 finds intact hollow-glyph tees; G4 then recovers assignment-missing tees from every fitting visible shard. Phantom tee completion remains OFF. The execution list is compiler chronology, while gate ownership is semantic rather than a waterfall.",
	"execution": [
		"badgeStage",
		"badges",
		"baskets",
		"tees",
		"teeFamily",
		"supportField",
		"badgeOcclusionPatch",
		"rawPairs",
		"measurement",
		"assignment",
		"teeRecovery",
		"zfit"
	],
	"gates": {}
}
