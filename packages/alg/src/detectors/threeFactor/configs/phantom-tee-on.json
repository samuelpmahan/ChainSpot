{
	"schema": "threeFactor-config@1",
	"name": "phantom-tee-on",
	"note": "C01 complete-occlusion fallback enabled: after assignment, tee-less holes get a tee synthesized at the predecessor hole's assigned basket tip, then assignment re-runs. Note the execution list gains the phantomTee unit — the order IS part of the experiment.",
	"execution": [
		"badgeStage",
		"badges",
		"baskets",
		"tees",
		"teeFamily",
		"supportField",
		"badgeOcclusionPatch",
		"rawPairs",
		"measurement",
		"assignment",
		"phantomTee",
		"zfit"
	],
	"gates": {
		"G4": {
			"phantomTee": { "enabled": true, "knobs": { "minViableScore": 0 } }
		}
	}
}
