{
	"schema": "threeFactor-config@1",
	"name": "phantom-tee-on",
	"note": "C01 complete-occlusion fallback enabled after frozen shard recovery: remaining tee-less holes get a tee synthesized at the predecessor hole's assigned basket tip, then assignment re-runs. Phantom completion remains an explicit default-OFF experiment.",
	"execution": [
		"badgeStage",
		"badges",
		"baskets",
		"tees",
		"teeFamily",
		"teeRecovery",
		"supportField",
		"badgeOcclusionPatch",
		"rawPairs",
		"measurement",
		"assignment",
		"phantomTee",
		"zfit"
	],
	"gates": {
		"G4": {
			"phantomTee": {
				"enabled": true,
				"knobs": {
					"minViableScore": 0
				}
			}
		}
	}
}
