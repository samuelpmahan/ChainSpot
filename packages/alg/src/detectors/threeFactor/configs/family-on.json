{
	"$schema": "./threeFactor-config.schema.json",
	"schema": "threeFactor-config@1",
	"name": "family-on",
	"note": "G2 cleanBasketFamily deviation with the baseline G3 intact visible tee family kept explicit in the plan. Shard recovery remains separate.",
	"execution": [
		"badgeStage",
		"badges",
		"baskets",
		"cleanBasketFamily",
		"tees",
		"teeFamily",
		"supportField",
		"badgeOcclusionPatch",
		"rawPairs",
		"measurement",
		"assignment",
		"zfit"
	],
	"gates": {
		"G2": {
			"cleanBasketFamily": {
				"enabled": true
			}
		},
		"G3": {
			"teeFamily": {
				"enabled": true
			}
		}
	}
}
