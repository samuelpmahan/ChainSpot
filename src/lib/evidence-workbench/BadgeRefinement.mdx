import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as BadgeStories from './BadgeEvidence.stories';

<Meta title="E/Badges/Refinement notebook" />

# Badge refinement notebook

One materialized E specimen is held in the global toolbar while the same projection component is reused across the sequence.

## B+W baseline

<Canvas of={BadgeStories.BrightAndDark} />

## Ownership

<Canvas of={BadgeStories.Ownership} />

## Naive AA hypothesis

<Canvas of={BadgeStories.AaCandidates} />

## Before/after composition

<Canvas of={BadgeStories.RefinementStack} />
