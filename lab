#!/usr/bin/env bash
# ChainSpot LAB entrypoint (POSIX). The npm package owns command parsing.
set -euo pipefail

repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec node "$repo_dir/scripts/chainspot-lab/bin/lab.mjs" "$@"
